% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/treatment_effects.R
\name{ate}
\alias{ate}
\title{Average Treatment Effect}
\usage{
ate(
  object,
  newdata = NULL,
  control = 0,
  treated = 1,
  se = TRUE,
  ci_level = 0.95,
  weights = NULL
)
}
\arguments{
\item{object}{A fitted \code{het_ensemble} object.}

\item{newdata}{Optional data frame for prediction. If NULL, uses training data.}

\item{control}{Value indicating the control condition (default 0).}

\item{treated}{Value indicating the treated condition (default 1).}

\item{se}{Logical. Compute standard error (default TRUE).}

\item{ci_level}{Confidence level for interval (default 0.95).}

\item{weights}{Optional observation weights for ATE calculation.}
}
\value{
A list of class \code{ate} with components:
\describe{
\item{estimate}{Point estimate of ATE}
\item{se}{Standard error (if se = TRUE)}
\item{ci_lower}{Lower confidence bound}
\item{ci_upper}{Upper confidence bound}
\item{ci_level}{Confidence level}
\item{n}{Sample size}
}
}
\description{
Computes the average treatment effect (ATE) from a fitted ensemble.
}
\examples{
\dontrun{
fit <- het_ensemble(y ~ x1 + x2 | treat, data = mydata)
ate(fit)
}
}
