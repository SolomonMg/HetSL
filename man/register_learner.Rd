% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learner_registry.R
\name{register_learner}
\alias{register_learner}
\title{Register a Custom Learner}
\usage{
register_learner(
  name,
  fit_fn,
  predict_fn,
  package = NULL,
  description = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{name}{Character. Unique name for the learner.}

\item{fit_fn}{Function. Fitting function with signature
\code{function(X, y, treatment, family, weights, ...)}.}

\item{predict_fn}{Function. Prediction function with signature
\code{function(model, newX, treatment, family)}.}

\item{package}{Character. Name of required package (optional, for documentation).}

\item{description}{Character. Description of the learner.}

\item{overwrite}{Logical. If TRUE, overwrite existing learner with same name.}
}
\value{
Invisible NULL.
}
\description{
Adds a custom learner to the registry for use with \code{het_ensemble}.
}
\examples{
\dontrun{
# Register a custom ridge regression learner
register_learner(
  name = "ridge",
  fit_fn = function(X, y, treatment, family, weights, ...) {
    glmnet::cv.glmnet(X, y, alpha = 0, family = family, weights = weights)
  },
  predict_fn = function(model, newX, treatment, family) {
    as.vector(predict(model, newX, s = "lambda.min", type = "response"))
  },
  package = "glmnet",
  description = "Ridge regression (L2 penalty)"
)
}
}
